<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.noodle.mapper.RankMapper">
<insert id="create">
insert into tbl_rank(name, year, rank)
values(#{name}, #{year}, #{rank})
</insert>

	<delete id="delete">
		delete from tbl_rank where #{name}
	</delete>
	
	<update id="update" parameterType="NoodleVO">
		update tbl_rank set
		name=#{name}, year=#{year}, component=#{component}, time=#{time}
	</update>
</mapper>