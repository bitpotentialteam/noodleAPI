<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.noodle.mapper.NutritionMapper">
<insert id="create">
insert into tbl_nutrition(name, calories, carbohydrate, protein, fat, saturatedfat, transfat, cholesterol, natrium)
values(#{name}, #{calories}, #{carbohydrate}, #{protein}, #{fat}, #{saturatedfat}, #{transfat}, #{cholesterol}, #{natrium})
</insert>

	<delete id="delete" parameterType="java.lang.String">
		delete from tbl_nutrition where #{name}
	</delete>

</mapper>