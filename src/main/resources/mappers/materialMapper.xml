<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.noodle.mapper.MaterialMapper">
<insert id="create">
insert into tbl_material(name, material_noodle, material_powder)
values(#{name}, #{material_noodle}, #{material_powder})
</insert>

	<delete id="delete">
		delete from tbl_material where name=#{name}
	</delete>
	
	<update id="update" parameterType="NoodleVO">
		update tbl_material set
		name=#{name}, material_noodle=#{material_noodle}, material_powder=#{material_powder}
	</update>

</mapper>