<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.noodle.mapper.RecipeMapper">
<insert id="create">
insert into tbl_recipe(name, water, minutes)
values(#{name}, #{water}, #{minutes})
</insert>

	<delete id="delete">
		delete from tbl_recipe where name=#{name}
	</delete>
	
	<update id="update" parameterType="NoodleVO">
		update tbl_recipe set
		name=#{name}, water=#{water}, minutes=#{minutes}
	</update>
 
</mapper>